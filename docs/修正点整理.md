# RENO 修正点整理

作成日：2026年8月28日

## 目的

現状の画面に対して上がってきた修正要望を整理し、実装前に認識を合わせる。
この文書では、対応内容を仮決めし、画面仕様や優先順位などの未確定事項を確認する。

## サイトURL

### 現在公開中のサイト

- 公開URL：<https://reno.taskra.jp/>
- LP：<https://reno.taskra.jp/lp.html>
- エージェント画面：<https://reno.taskra.jp/agent.html>

現在公開中のサイトは、React版公開前の仮ページです。

### React版公開後の確認URL

- Reactメインページ：CloudFront公開URL（未確定）
- CloudFrontドメイン：未確定
- カスタムドメイン切り替え後：<https://reno.taskra.jp/>

React版を公開した後は、上記URLで以下を確認する。

- React版の画面が表示されること
- PC・スマートフォンの表示が切り替わること
- API接続が正常に動作すること
- LPとサービス画面が分離されていること

### 本番公開の対象

今回は画面だけを公開するのではなく、動的機能を含む本番用構成として公開する。

- React版メイン画面
- API Gateway + Lambdaによるチャット・診断・相談受付
- DynamoDBへのセッション・相談情報保存
- S3への画像・生成ファイル保存
- Cognitoによる認証基盤
- CloudFront + S3によるフロントエンド配信

公開にはGitHub Actionsの `Deploy full MVP` を使用する。バックエンドをデプロイし、発行されたAPI URLをフロントエンド設定へ反映したうえで、React版をCloudFrontへ配信する。

本番公開後は、画面表示だけでなく、チャット、画像アップロード、画像診断、相談受付、データ保存まで動作確認する。

## 修正一覧

| No. | 修正内容 | 現時点の認識 | 状態 |
|---|---|---|---|
| 1 | PIN入力を削除 | ユーザーがサービスを利用開始する際のPIN入力・PIN認証画面をなくし、PINなしで利用できる導線にする | 要確認 |
| 2 | PC表示に対応 | 現在のスマートフォン中心のレイアウトを、PCの画面幅でも自然に利用できるレスポンシブレイアウトへ拡張する | 要確認 |
| 3 | メニューを入力バー上部へ配置 | 右上にあるメニュー等の主要操作を、ユーザーが見つけやすく操作しやすいよう、チャット入力バーの上部付近へ移動・再配置する | 要確認 |
| 4 | PC表示時のQRコード常駐 | PCで表示している場合に、スマートフォンで開くためのQRコードを常時表示する。スマートフォン表示時はQRコードを表示しない | 要確認 |
| 5 | LPページとサービス画面を分離 | サービス利用画面とLP（サービス紹介・訴求）ページが混在しているため、役割・導線・画面構成を分ける | 要確認 |

## 詳細整理

### 1. PIN入力を削除

#### 仮の対応方針

- 初回表示時のPIN入力画面を削除する。
- PIN入力を前提としているボタン、説明文、モーダル、エラーメッセージを整理する。
- PIN認証が管理者向け機能や別用途でも使われている場合は、ユーザー向け導線だけを削除し、管理者機能は別途維持する。

#### 確認事項

- 削除対象は、一般ユーザーがサービスを開始する際のPIN入力だけか。
- 管理者ログイン、ゲストPIN発行・検証などの機能も削除するのか。
- PINなしの場合、利用開始時に必要な認証や同意確認は残すのか。

### 2. PC表示への対応

#### 仮の対応方針

- スマートフォン表示を基準にしつつ、PCでは横幅を有効活用する。
- チャット画面、メニュー、画像表示、モーダル、入力エリアがPC幅で崩れないようにする。
- PCではコンテンツの最大幅を設定し、過度に横へ広がらないレイアウトにする。
- 画面幅に応じて、ナビゲーションや補助情報の表示方法を切り替える。

#### 確認事項

- 対応するPC画面幅の目安（例：1280px、1440px）を決めるか。
- タブレット表示も今回の対象に含めるか。
- PCではチャット画面を中央寄せにするか、左右に補助エリアを設けるか。

### 3. メニューの再配置

#### 仮の対応方針

- 右上に分散しているメニューや主要操作を、チャット入力バーの上部にまとめる。
- ユーザーが入力前後に確認・操作しやすい位置に配置する。
- メニュー項目の優先度を整理し、頻繁に使う操作を優先して表示する。
- PC・スマートフォンの両方で、入力操作を妨げない配置にする。

#### 確認事項

- 「右上メニュー表示など」に含める具体的なメニュー項目は何か。
- メニューを常時展開するのか、ボタン押下で開くのか。
- 入力バー上部に配置する対象は、全メニューか、主要メニューだけか。

### 4. PC表示時のQRコード常駐

#### 仮の対応方針

- PC表示時のみ、「スマホで開く」ためのQRコードを画面内に常駐させる。
- QRコードはチャット操作を邪魔しない固定位置に配置する。
- スマートフォン表示時はQRコードを非表示にする。
- QRコードの遷移先は、現在表示しているサービス画面のURLとする。

#### 確認事項

- QRコードは画面右側に固定するか、入力バー上部などに置くか。
- PC表示時に常に表示するか、一定時間後に折りたたむか。
- QRコードの横に「スマホで開く」などの説明を表示するか。
- QRコードの遷移先をトップ画面にするか、現在の画面・セッションを引き継ぐURLにするか。

### 5. LPページとサービス画面の分離

#### 仮の対応方針

- LPはサービスの説明、特徴、料金、導入メリット、利用開始への誘導を担当する。
- サービス画面は、チャット・画像診断・相談などの利用機能に集中させる。
- LP内のサービス画面要素や、サービス画面内のLP的な説明を整理する。
- 両者を明確な別URL・別導線として扱う。
- 「サービスを試す」「ログインする」など、LPからサービス画面へ遷移する入口を整理する。

#### 確認事項

- LPとサービス画面をURLとして完全に分離するのか。
- サービス画面内に最低限のサービス説明を残すのか。
- LPを最初に表示し、そこからサービスへ誘導する構成にするのか。
- 現在存在するLP、モックアップ、収益構造ページの扱いをどうするか。

## 実装前の認識合わせ項目

以下を確定してから画面修正に着手する。

1. PIN削除の対象範囲（一般ユーザーのみ／管理者機能も含む）
2. PC画面の基本レイアウト
3. 入力バー上部へ移動するメニュー項目
4. PC時QRコードの固定位置と遷移先
5. LPとサービス画面のURL・導線

## 確定内容の記録

| No. | 確定内容 | 確定日 |
|---|---|---|
| 1 | 未確定 | - |
| 2 | 未確定 | - |
| 3 | 未確定 | - |
| 4 | 未確定 | - |
| 5 | 未確定 | - |
